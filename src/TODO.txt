


> JOIN NODE HANDLERS MERGING
Make a parent class PairwiseMergeJoinNodeHandler with method merge that takes two colourings and merges them
This could make implementing easier

> EXACT TREE DECOMPOSITION SOLVER

> DYNAMIC ORDER JOIN HANDLER
cache the eval of vertices in bag + update in stead of recomputing each time